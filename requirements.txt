# Loose bounds for the notebook environment (Python 3.13); the exact executed
# versions are pinned in requirements-lock.txt, which also explains how this
# environment relates to the replay run's environment (recorded in
# results/replay/replay_manifest.json).
# The notebooks also import the research library from a sibling checkout of
# the (private) qfdev_research repo; see the README for what is and is not
# redistributed.
numpy>=2.0
pandas>=2.2
pyarrow>=17
matplotlib>=3.9
scipy>=1.14
scikit-learn>=1.6
lightgbm>=4.5
torch>=2.6
